"""SCAR implementation package.

SCAR learns transition-side continuous latent actions with an inverse dynamics
model and uses them to condition a Wan-based forward dynamics model. The package
contains reusable model components, objectives, data loading, evaluation, and
runtime utilities; runnable experiment entry points live in ``scripts/``.
"""

__all__: list[str] = []
